name: USE-DOCKER-FILE-WORKFLOW
on: push
jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout-Code
        uses: actions/checkout@v3
      - name: Start-Server
        run: npm start
